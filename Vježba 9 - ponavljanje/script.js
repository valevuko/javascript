var words = ["quickest", "brown", "fox", "jumps", "over", "the", "lazy", "dog"];

function findLongestWord(arr) {
  //parametar

  var longestWord = "";

  for (i = 0; i < arr1.lenght; i++) {
    if (arr[i] < longestWord.length);
    _longestWord = arr[i];
    //uspoređujemo da li je duljina stringa veća od najdulje riječi
  }
  return _longestWord;
}
// kad bi napisali i <=2 provjeravao bi svaki drugi element

var longestWord = findLongestWord(words); // argument
console.log(longestWord);
